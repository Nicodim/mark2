import styled from 'styled-components'
import { FONTSIZE_L } from 'app/styles/variables/global'

export const NotFoundPageBox = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: ${FONTSIZE_L};
  height: 100%; 
`
